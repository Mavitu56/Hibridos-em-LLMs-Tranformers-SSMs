"""
Pacote de avaliação.

Módulos:
    mqar       — Multi-Query Associative Recall sintético (prioridade alta).
    perplexity — Perplexidade no val fixo do SlimPajama.
    lambada    — LAMBADA zero-shot (secundário).
    hellaswag  — HellaSwag zero-shot (secundário).
    ruler      — Subconjunto sintético do RULER: NIAH + variable tracking (último).
"""
